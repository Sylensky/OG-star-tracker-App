// Empty stub used so VS Code can run a preLaunchTask and keep a debug session open.
// This file intentionally does nothing. The real work is done by the preLaunchTask (adb logcat).

setInterval(() => {}, 1000);
